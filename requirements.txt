# Mixed-Effect Thompson Sampling - dependencies
# Install: pip install -r requirements.txt
# Optional: run `pip freeze > requirements.txt` in your venv to pin exact versions.

numpy>=1.19
scipy>=1.5
matplotlib>=3.2
joblib>=1.0
scikit-learn>=0.24
